// ts config for the tests
{
    "compilerOptions": {
        "types": ["jest", "node"],
        "noEmit": true,
        "strict": true,
        "noImplicitThis": true,                           /* Enable error reporting when 'this' is given the type 'any'. */
    }
}